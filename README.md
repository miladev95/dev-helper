# Development Helper

## Bash aliases and commands
### some commands for `git`, `laravel`, `shell`, `golang`, `curl`,...
how to use :

`echo bashrc >> ~/.bashrc`

`source ~/.bashrc`

---

## Initial ubuntu
### install IDE,... in ubuntu
how to use:

`sh init.sh`

---